@using MudBlazor
@inherits MudForm

<MudForm Model="@ViewObject" Validation="@(_entityNameValidator.ValidateValue)" ValidationDelay="0">
  <MudCardContent>

    <MudTextField @bind-Value="ViewObject.Id"
                  For="@(() => ViewObject.Id)"
                  Immediate="true"
                  Label="Id" />

@*     <MudTextField @bind-Value="ViewObject.CreatedAt"
                  For="@(() => ViewObject.CreatedAt)"
                  Immediate="true"
                  Label="CreatedAt" />

    <MudTextField @bind-Value="ViewObject.UpdatedAt"
                  For="@(() => ViewObject.UpdatedAt)"
                  Immediate="true"
                  Label="UpdatedAt" /> *@

    @* // TODO - Complete with other fields *@

  </MudCardContent>

  @* // TODO - Complete with other sub items fields *@
  @* <MudCardContent Class="pa-0">

  <MudTable Items="@Vo.SubItems" Hover="true" Breakpoint="Breakpoint.None" Dense="@true" Elevation="0">
  <HeaderContent>


  </HeaderContent>
  <RowTemplate>


  </RowTemplate>
  </MudTable>

  </MudCardContent> *@
</MudForm>
@{
  base.BuildRenderTree(__builder);
}